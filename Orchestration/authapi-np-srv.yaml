apiVersion: v1
kind: Service
metadata:
  name: auth-np-srv
spec:
  type: NodePort
  selector:
    app: jacko-auth-service
  ports:
    - name: jacko-auth-service
      protocol: TCP
      port: 8080
      targetPort: 8080
