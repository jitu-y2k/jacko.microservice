apiVersion: apps/v1
kind: Deployment
metadata:
  name: jacko-order-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jacko-order-service
  template:
    metadata:
      labels:
        app: jacko-order-service
    spec:
      containers:
        - name: jacko-order-service
          image: jituy2k/order-service:latest
---
apiVersion: v1
kind: Service
metadata:
  name: jacko-order-clusterip-srv
spec:
  type: ClusterIP
  selector:
    app: jacko-order-service
  ports:
  - name: jacko-order-service
    protocol: TCP
    port: 8080
    targetPort: 8080

