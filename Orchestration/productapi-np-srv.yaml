apiVersion: v1
kind: Service
metadata:
  name: product-np-srv
spec:
  type: NodePort
  selector:
    app: jacko-product-service
  ports:
    - name: jacko-product-service
      protocol: TCP
      port: 8080
      targetPort: 8080
